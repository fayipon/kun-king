# 登入／註冊關閉按鈕位置與樣式

- 狀態：已完成實作、驗證及部署；使用者本次直接要求依截圖移動並統一樣式，授權開始。
- 將兩頁 Header 的 X 移至表單卡片右上角（截圖下方紅框）。
- 使用者更正：所指為 Welcome Rewards 彈窗的帶圓框 X，而非服務說明彈窗。依本次指示直接修正：30px 圓框、淡紫邊線、深紫半透明底與淡紫 X，位置保持在表單卡片右上角。
- 點擊仍返回前台首頁。卡片頂部預留空間，避免與登入標題／註冊輸入欄重疊。
- 驗證登入／註冊、320px／560px、返回操作、既有測試與正式建置；提交並部署，記錄 Finish/028。

## 驗收

1. 兩頁僅有一個頁面關閉 X，位於卡片內右上角，Header 不再顯示。
2. X 與彈窗共用樣式；窄螢幕不遮蓋內容，鍵盤可操作，點擊返回前台。
3. 測試及建置通過，線上兩個路由皆更新。
