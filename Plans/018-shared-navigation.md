# 共用 Header 與底部導覽

- 狀態：範圍已確認，待審核及開始；尚未實作。
- 現況：Landing、Promo各自實作Header及底部導覽；Auth有獨立Header。樣式、寬度與入口行為存在重複。

## 範圍

使用者確認：所有前台內容頁共用，包含 #/frontend、#/promo、#/play 及後續新增前台內容頁。登入／註冊保留目前布局；項目入口與後台不套用。Godot示範頁改置於共用前台框架內，保留原資源檢查及啟動行為。

## 需求與執行

1. 建立共用FrontendLayout、Header與BottomNav，路由集中決定顯示及選中項目，避免每頁複製元件與CSS。
2. Header統一品牌、返回、Log In／Register、置頂高度、間距及顏色；返回目標以明確參數配置。登入／註冊若保留現有布局，其X返回首頁行為不變。
3. 底部統一Home／Promo／Wallet／Affiliate／My、中央Wallet樣式、選中狀態及入口行為。
4. 內容、Header與BottomNav共用480px最大寬度及手機自適應規則；底部安全區與內容預留間距集中管理。
5. 將Wallet／Affiliate／My的共用面板與帳號連結集中處理；My Favorites導向首頁收藏。保留各頁專屬活動／遊戲面板。
6. 首頁歡迎彈窗仍僅在首頁觸發；刷新顯示規則由Plans/017另行核准。
7. 檢查路由切換、返回、分類、收藏、面板焦點與捲動恢復；更新測試、README、Finish，commit、push並部署驗證。

## 驗收與用例

| 編號 | 操作 | 預期結果 |
| --- | --- | --- |
| S01 | 在納入範圍的頁面間切換 | 共用Header／BottomNav外觀及寬度一致，無跳動 |
| S02 | Home與Promo切換、直連及刷新 | 正確頁面與選中項目，無重複Header／導覽 |
| S03 | Wallet／Affiliate／My及收藏 | 所有頁面使用同一套入口行為 |
| S04 | 登入／註冊／返回操作 | 路由正確，依確認範圍套用布局 |
| S05 | 320／390／480px及桌面 | 無溢出，固定Header及導覽不遮蔽主要操作 |
| S06 | 面板開關、Esc、鍵盤Tab | 焦點約束與返回、背景捲動正常 |
| S07 | 測試、建置及線上驗證 | 通過，Finish記錄實測結果 |
