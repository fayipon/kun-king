# Affiliate 邀請獎勵頁 — 實作與驗證

- 對應：[Plans/027](../Plans/027-affiliate-page.md)
- 日期：2026-09-11；Windows、Node/Vite、Vitest/jsdom、Codex 內建 Chromium 瀏覽器。
- 實作完成，部署驗證待推送後補記。

## 完成範圍

新增 `#/affiliate`，接入共用 Header／BottomNav／標題及路由捲動處理。移除 Affiliate 準備中面板。頁面包含三欄統計、旅程、進度、兩個操作、最近邀請與完整示範清單。

資料集中管理：12 位有效好友，5／10 門檻達成、2 個可預覽寶箱，15 門檻還差 3 人，80% 進度。固定樣本日期 2026-09-11。邀請碼與註冊連結是示範，沒有推薦綁定；開箱沒有發獎、扣箱或變更 Wallet。

原生 dialog 支援 X、Esc、遮罩、Tab／Shift+Tab 循環、捲動鎖與原開啟按鈕焦點回復。從完整清單切到好友詳情後，關閉仍回到外部 View all。複製非同步成功後才提示，拒絕或不可用時保留唯讀可選取文字；過期複製結果不污染重開面板。

## 自動測試

- `npm test`：7 個檔案、57 項全部通過。
- `npm run build`：TypeScript 與 Vite 正式建置通過。
- 新增 9 項 Affiliate 測試，涵蓋直連、跨頁導覽、數據、五個門檻、複製成功／拒絕／不存在／延遲完成、登入註冊、兩個開箱、Wallet 不變、12 人清單、固定日期、焦點循環與回復。
- 更新既有 Landing 測試，由 Affiliate 準備中面板改為獨立頁驗證。

## 瀏覽器與驗收

| 項目 | 結果與佐證 |
| --- | --- |
| A01 | 本地直連、重新整理、導覽與單組共用框架通過；線上待部署補記。 |
| A02–A03 | 頁面各區、12／2／3、12/15、五個節點與初始狀態通過。 |
| A04 | 規則及所有節點自動測試通過；瀏覽器確認規則面板。 |
| A05 | 瀏覽器複製顯示 Copied successfully；拒絕／不可用與延遲結果由測試驗證。 |
| A06 | 登入／註冊路由與捲動解除測試通過。 |
| A07 | 5／10 開箱預覽測試通過，瀏覽器確認 10 invites 顯示未發獎；Wallet 維持 ₱13,300.00。減少動態見 A15。 |
| A08 | 12 位完整清單通過；瀏覽器 MariaPlay 詳情顯示 2026-09-08、Valid、樣本日期。 |
| A09 | X、Esc、遮罩及焦點回復通過；實際發現末尾 Tab 可到瀏覽器外部焦點後，加入雙向循環，瀏覽器重測確認焦點在 Close affiliate panel。 |
| A10 | 320／390／480px 與 1280px 寬檢查，無水平溢出。桌面內容最大480px。320px 將節點最小高度調為32px，避免相鄰熱區重疊；文字、按鈕、好友列與安全留白確認。 |
| A11 | 57 項測試及建置通過；部署待補記。 |
| A12 | 已實作2.6秒局部亮光與錯開光點；遮罩扣除金箱本體與數字。靜態畫面已確認；尚未完成連續兩轮的人工動態觀察／錄影。 |
| A13 | 已實作5秒旗幟字樣色彩變化與柔光；從原像素產生遮罩，旗布不變。靜態對位已確認；尚未完成連續兩輪的人工動態觀察／錄影。 |
| A14 | 原圖與實作截圖已目視對照，底圖使用完全相同像素及viewBox，沒有重生成的構圖偏移。獨立局部截圖未在本次素材中找到，該部分尚不能驗收。 |
| A15 | 沿用全站 `prefers-reduced-motion:reduce` 停用動畫，另加靜態高亮。程式規則已檢查；尚未在作業系統切換偏好後實機驗證。 |

## 原圖對照與差異

原图尺寸941×1672；地圖裁切 `(95,302,752,662)`。五個寶箱中心依原座標 `(485,880)`、`(403,771)`、`(450,544)`、`(474,443)`、`(511,339)`，雞頭像 `(490,661)`。城堡、浮島、路徑、裝飾與旗布均沿用原素材。

文字改為程式繪製，字型及字級與原稿略異；原數字背景以取色柔邊小底板覆蓋，因此局部背景不屬於逐像素一致。Header 與底部 Wallet 依既有規格；手機標籤可換行，移除原圖多餘頁尾空白。素材與遮罩方法見 [Design/affiliate](../Design/affiliate/README.md)。

## 限制

僅前端示範。尚無實際邀請資格、獎勵規則、推薦 API 或帳戶服務。上述動態人工觀察、作業系統減少動態實測及缺少的兩張局部參考，未冒稱通過。
